# Configuration pour la production
# Usage: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
services:
  frontend:
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://69.197.142.189:5022
      - NEXT_PUBLIC_WS_URL=ws://69.197.142.189:5022
    ports:
      - "5020:3000"  # Port production
  
  backend:
    environment:
      - NODE_ENV=production
      - PORT=3001
      - WS_PORT=3002
      - DB_PATH=/app/data/notifications.db
    ports:
      - "5022:3001"  # Port API REST production
      - "5023:3002"  # Port WebSocket natif production
